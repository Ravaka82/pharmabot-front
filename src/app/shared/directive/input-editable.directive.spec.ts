import { InputEditableDirective } from './input-editable.directive';

describe('InputEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new InputEditableDirective();
    expect(directive).toBeTruthy();
  });
});
